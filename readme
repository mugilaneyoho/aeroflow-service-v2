planed services:
    authentication: (started)
        -master admin (CRUD done) (login done)
        -sub admin (added MA)
        -department admin (added MA)
        -staff (start)
        -student
        -telecaller
        -reception
    institute: (completed CRUD)
        -profile
        -branch
        -course
        -batch
    training:
        -staff
        -student
        -class
        -attendance (option to create other service as feature)
    telecalling: ()
        -employee (completed CRUD)
        -leads (completed upload and assigned)
    reception: (adarsh)
        -profile
        -meetting
    payment:
        -CRUD
    filesystem:
        -CRUD
    notifyandlogs:
    placement:
    ticket-system:
    mailservice:
    openvidu:

add techs:
    grpc
    redis
    bull
    rabbitmq
    postgres
